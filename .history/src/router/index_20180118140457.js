import Vue from 'vue'
import Router from 'vue-router'
import Pic from '@/components/pic'
Vue.use(Router)


export function createRouter () {
  return new Router({
    mode: 'history',
    routes: [
     
    ]
  })
}
